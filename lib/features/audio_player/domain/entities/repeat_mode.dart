/// Repeat mode options for audio playback
enum RepeatMode {
  /// No repeat - play queue once and stop
  none,

  /// Repeat current track indefinitely
  single,

  /// Repeat entire queue when it reaches the end
  queue,
}
